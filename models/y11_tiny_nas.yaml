# Auto-generated YOLO11-style tiny model
nc: 1
depth_multiple: 1.0
width_multiple: 0.25

backbone:
  # [from, number, module, args]
  - [-1, 1, GhostConv, [64, 3, 2]]          # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]              # 1-P2/4
  - [-1, 1, C2f, [128, True]]           # 2
  - [-1, 1, GhostConv, [256, 3, 2]]         # 3-P3/8
  - [-1, 1, C2f, [256, True]]           # 4  (P3)
  - [-1, 1, GhostConv, [512, 3, 2]]         # 5-P4/16
  - [-1, 1, C2f, [512, True]]           # 6  (P4)
  - [-1, 1, GhostConv, [1024, 3, 2]]        # 7-P5/32
  - [-1, 1, C2f, [1024, True]]                 # 8
  - [-1, 1, SPPF, [1024, 5]]                   # 9  (P5)

head:
  - [-1, 1, Conv, [512, 1, 1]]                  # 10
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11  /32->/16
  - [[-1, 6], 1, Concat, [1]]                   # 12  (up + P4)
  - [-1, 1, C2f, [512]]                   # 13  N4

  - [-1, 1, Conv, [256, 1, 1]]                  # 14
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 15  /16->/8
  - [[-1, 4], 1, Concat, [1]]                   # 16  (up + P3)
  - [-1, 1, C2f, [256]]                   # 17  N3 (small)

  - [-1, 1, GhostConv, [512, 3, 2]]          # 18  /8->/16
  - [[-1, 13], 1, Concat, [1]]                  # 19  (down + N4)
  - [-1, 1, C2f, [512]]                   # 20  N4 (mid)

  - [-1, 1, GhostConv, [1024, 3, 2]]         # 21  /16->/32
  - [[-1, 9], 1, Concat, [1]]                   # 22  (down + P5)
  - [-1, 1, C2f, [1024]]                  # 23  N5 (large)

  - [[17, 20, 23], 1, Detect, [1]]           # 24 Detect(P3,P4,P5)
